const root_dir = '../out'
const src_dir = `<rootDir>/..`
const out_dir = `<rootDir>`
const compiled_dir = `<rootDir>/compiled`
const jest_dir = `${src_dir}/build-tools`

module.exports = {
  root_dir,
  out_dir,
  compiled_dir,
  jest_dir,
  src_dir,
}
